# =============================================================================
# Notes — write in English only (no Chinese here).
#
# This file is NOT an archive. Published posts live only in posts/<id>.json and
# posts/manifest.json. Do not keep old post blocks in this file; the sync
# command removes them after a successful run (these # lines are kept).
#
# Publish a new or updated post:
#   1) Add one post block below: start a line with [[POST]], your headers
#      (title, optional id, optional date), a blank line, the English body, then
#      a line with [[/POST]]. See scripts/sync-drafts.mjs for the exact format.
#   2) Run:  node scripts/sync-drafts.mjs
#   3) The script merges that block into the JSON on disk, updates the manifest,
#      and clears all such blocks from this file.
#   4) Add Chinese (zhTitle / zhBody) in posts/<id>.json if missing; then commit
#      and push. Re-run sync any time; existing zh is preserved when English changes.
#
# Headers (one per line, then a blank line, then the body):
#   id:     optional — URL slug; auto from title if omitted. If it matches an
#           existing post id, that post is updated in English.
#   date:   optional — ISO time, e.g. 2026-04-16T12:00:00.000Z (defaults to now)
#   title:  required
#
# For AI / coding agents: read instructions-for-ai.txt in the repository root
# (not this file) for the full contract. Do not re-fill this file with all old
# posts. Backstage (after login) has a short “Instructions for AI” panel.
# =============================================================================
